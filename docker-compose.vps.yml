name: fj

services:
  app:
    image: ${APP_IMAGE:-fj-app:local}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GAME_RELEASE_VERSION: ${GAME_RELEASE_VERSION:-}
        GITHUB_RUN_NUMBER: ${GITHUB_RUN_NUMBER:-}
        GITHUB_SHA: ${GITHUB_SHA:-}
        BUILD_NUMBER: ${BUILD_NUMBER:-}
        COMMIT_SHA: ${COMMIT_SHA:-}
        SOURCE_COMMIT: ${SOURCE_COMMIT:-}
        SOURCE_VERSION: ${SOURCE_VERSION:-}
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
        NEXT_PUBLIC_SENTRY_ENVIRONMENT: ${NEXT_PUBLIC_SENTRY_ENVIRONMENT:-production}
        NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE: ${NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE:-0.1}
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env.vps
    environment:
      HOST: 127.0.0.1
      WEB_PORT: 3011
      DATABASE_URL: postgresql://${POSTGRES_USER:?set POSTGRES_USER}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@127.0.0.1:5432/${POSTGRES_DB:?set POSTGRES_DB}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/healthz | grep -q '\"status\":\"ok\"'"]
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 60s

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?set POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:?set POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:?set POSTGRES_USER} -d ${POSTGRES_DB:?set POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    network_mode: host
    environment:
      APP_PUBLIC_HOST: ${APP_PUBLIC_HOST:?set APP_PUBLIC_HOST}
    volumes:
      - ./deploy/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      app:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    profiles:
      - redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  postgres-data:
  caddy-data:
  caddy-config:
  redis-data:
